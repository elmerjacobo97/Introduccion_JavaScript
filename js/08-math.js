let resultado;
resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.2); // ceil redondea siempre hacia arriba
resultado = Math.floor(2.9); // floor redondea siempre hacia abajo
resultado = Math.sqrt(144); // raíz cuadrada
resultado = Math.abs(-200); // negativo a positivo
resultado = Math.min(3, 5, 6, 1, 10, 40); // número mínimo
resultado = Math.max(3, 5, 6, 1, 10, 40); // número máximo
resultado = Math.random(); // número aleatorio
resultado = Math.floor(Math.random() * 30) // número aleatorio (distinto)

console.log(resultado);

